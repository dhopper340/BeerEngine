import { EmployeeListResolver } from './employee-list.resolver';

describe('Resolver', () => {
  it('should create an instance', () => {
    expect(new EmployeeListResolver(null)).toBeTruthy();
  });
});
