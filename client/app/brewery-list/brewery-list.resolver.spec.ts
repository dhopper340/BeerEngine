import { BreweryListResolver } from './brewery-list.resolver';

describe('Resolver', () => {
  it('should create an instance', () => {
    expect(new BreweryListResolver(null)).toBeTruthy();
  });
});
